# NZP Workshop CI Demonstration

A minimal Node test project used to demonstrate a pull-request-triggered GitHub Actions workflow and its test-output artifact.

The UAT workflow runs the reviewed address-finder checks and uploads its report artifacts on a pull request.
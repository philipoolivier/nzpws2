# My UAT test plan

Complete each row before generating its test. These are human review notes, not switches that turn tests green.

| ID | Input | Expected visible outcome | Evidence/source | Read-only actions reviewed by me | Test file and actual run result |
|---|---|---|---|---|---|
| U01 | 122 kerwyn | Observation, not a requirement: the visible matching suggestions were `122 Kerwyn Avenue` / `East Tamaki` / `Auckland 2013` / `Postcode` / `2013`; `122A Kerwyn Avenue` / `East Tamaki` / `Auckland 2013` / `Postcode` / `2013`. | Playwright MCP, 2026-09-07: input accessible name `Address search input`; read-only UAT search at `https://uat.nzpost.co.nz/tools/address-postcode-finder`. | Opened the page and typed `122 kerwyn`; did not select a suggestion or submit the form. | No test created. |
| U02 | Pay a bill | Observation, not a requirement: seeded the page with Quick search `Auckland`; after choosing `Pay a bill`, the visible filter label was `Filter by: Pay a bill`; after choosing `All`, the visible cleared label was `Filter by`. | Playwright MCP, 2026-09-07: read-only UAT observation at `https://uat.nzpost.co.nz/tools/find-nz-post/Auckland`; the location textbox displayed `Auckland`; accessible button names were `Filter by: Pay a billFilter Icon` when applied and `Filter byFilter Icon` when cleared. | Chose Quick search `Auckland`, opened the filter, chose `Pay a bill`, recorded its label, then chose `All` and recorded the cleared label. Did not type in the location field, select a result, submit a form, or change records. | No test created. |
| U03 | 172863 aksdhj | Observation, not a requirement: in a repeated fresh same-session observation, replacing `122 kerwyn` with `172863 aksdhj` removed the earlier `122 Kerwyn Avenue` and `122A Kerwyn Avenue` rows. The finder retained an empty list with no suggestion, result, or list-item rows. | Playwright MCP, 2026-09-07: input accessible name `Address search input`; read-only UAT search at `https://uat.nzpost.co.nz/tools/address-postcode-finder`. | Typed `122 kerwyn` and recorded the two visible suggestion rows, then replaced the input with `172863 aksdhj`. Did not select a suggestion or submit the form. | No test created. |
| U04 | Private Bag 37006, Te Karaka 4042 | Observation, not a requirement: one visible matching result showed `Private Bag 37006`, `Te Karaka 4042`, `Postcode`, and `4042`. | Playwright MCP, 2026-09-07: input accessible name `Address search input`; read-only UAT search at `https://uat.nzpost.co.nz/tools/address-postcode-finder`. | Typed `Private Bag 37006, Te Karaka 4042`; did not select a suggestion or submit the form. | No test created. |
| U05 | PO Box 101, Paihia 0247 | Observation, not a requirement: one visible matching result showed `PO Box 101`, `Paihia 0247`, `Postcode`, and `0247`. | Playwright MCP, 2026-09-07: input accessible name `Address search input`; read-only UAT search at `https://uat.nzpost.co.nz/tools/address-postcode-finder`. | Typed `PO Box 101, Paihia 0247`; did not select a suggestion or submit the form. | No test created. |
| U06 | Auckland Central, Auckland | Observation, not a requirement: one visible matching result showed `Auckland Central` and `Auckland` together in the same result. | Playwright MCP, 2026-09-07: input accessible name `Address search input`; read-only UAT search at `https://uat.nzpost.co.nz/tools/address-postcode-finder`. | Typed `Auckland Central, Auckland`; did not select a suggestion or submit the form. | No test created. |

U02 is a separate Find NZ Post service-filter route, so confirm its route and seed search before
authoring. All six customer cases are workshop requirements. All expected outcomes and evidence remain blank
until observed and reviewed.
